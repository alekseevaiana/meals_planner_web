function Title({ children }) {
  return <h1>{children}</h1>;
}

function Groceries({ ingredients }) {
  return (
    <>
      <Title>Groceries</Title>
      {/* 
      - fetch ingredients from outer API and put to users ingredients (fetch when create account and add user ids)
      - fetch ingredients from Amplify
      - concatenate ingredients together and sort
      - search input
      - categories
      - checkboxes with ingredients
      - every checkbox has delete btn
      - delete checked items 
      */}
    </>
  );
}
